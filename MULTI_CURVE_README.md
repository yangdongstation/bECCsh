# 🚀 bECCsh 多椭圆曲线支持指南

## 📋 概述

bECCsh 现在支持 **9种标准椭圆曲线算法**，从轻量级的96位安全级别到最高256位安全级别，满足不同应用场景的需求。

## 🎯 支持的椭圆曲线

### 🔴 NIST标准曲线 (美国政府标准)

| 曲线名称 | 别名 | 安全级别 | 密钥长度 | 主要用途 |
|---------|------|----------|----------|----------|
| **secp192k1** | - | 96位 | 192位 | 轻量级应用、物联网设备 |
| **secp224k1** | - | 112位 | 224位 | 比特币早期使用、中等安全 |
| **secp256r1** | P-256, prime256v1 | 128位 | 256位 | TLS 1.3、JWT、政府标准 |
| **secp384r1** | P-384, prime384v1 | 192位 | 384位 | 高安全性应用、企业级 |
| **secp521r1** | P-521, prime521v1 | 256位 | 521位 | 最高安全级别、长期保密 |

### 🟡 Koblitz曲线 (高效计算)

| 曲线名称 | 安全级别 | 密钥长度 | 特点 | 主要用途 |
|---------|----------|----------|------|----------|
| **secp256k1** | 128位 | 256位 | 计算效率高 | 比特币、以太坊等加密货币 |

### 🟢 Brainpool曲线 (欧洲标准)

| 曲线名称 | 安全级别 | 密钥长度 | 特点 | 主要用途 |
|---------|----------|----------|------|----------|
| **brainpoolp256r1** | 128位 | 256位 | 参数透明、可验证 | 欧洲标准、高透明度 |
| **brainpoolp384r1** | 192位 | 384位 | 参数透明、高安全 | 欧洲高安全标准 |
| **brainpoolp512r1** | 256位 | 512位 | 参数透明、最高安全 | 欧洲最高安全标准 |

## 🚀 快速开始

### 1. 查看支持的曲线

```bash
# 显示所有支持的曲线信息
./becc_multi_curve.sh curves

# 获取智能曲线推荐
./becc_multi_curve.sh recommend
```

### 2. 生成密钥对

```bash
# 使用默认曲线 (secp256r1)
./becc_multi_curve.sh keygen -f my_key.pem

# 使用比特币标准曲线
./becc_multi_curve.sh keygen -c secp256k1 -f bitcoin_key.pem

# 使用高安全性曲线
./becc_multi_curve.sh keygen -c secp521r1 -f high_security_key.pem

# 使用欧洲标准曲线
./becc_multi_curve.sh keygen -c brainpoolp256r1 -f european_key.pem
```

### 3. 签名和验证

```bash
# 签名消息
./becc_multi_curve.sh sign -c secp256r1 -k private_key.pem -m "Hello World" -f signature.der

# 验证签名
./becc_multi_curve.sh verify -c secp256r1 -k public_key.pem -m "Hello World" -s signature.der
```

### 4. 批量测试

```bash
# 测试所有曲线
./test_multi_curve.sh

# 性能基准测试
./benchmark_multi_curve.sh -n 100

# 测试特定曲线性能
./benchmark_multi_curve.sh -c secp256k1 -n 50
```

## 🎯 智能曲线推荐

### 按安全级别选择

```bash
# 128位安全级别 (标准)
./becc_multi_curve.sh recommend --security 128

# 192位安全级别 (高安全)
./becc_multi_curve.sh recommend --security 192

# 256位安全级别 (最高安全)
./becc_multi_curve.sh recommend --security 256
```

### 按用例选择

```bash
# 移动/物联网应用
./becc_multi_curve.sh recommend --use-case mobile

# 加密货币应用
./becc_multi_curve.sh recommend --use-case bitcoin

# Web/TLS应用
./becc_multi_curve.sh recommend --use-case web

# 企业级应用
./becc_multi_curve.sh recommend --use-case government

# 长期存档
./becc_multi_curve.sh recommend --use-case long-term
```

### 综合推荐

```bash
# 平衡性能和安全性
./becc_multi_curve.sh recommend --security 128 --performance balanced

# 性能优先
./becc_multi_curve.sh recommend --security 128 --performance fast

# 安全优先
./becc_multi_curve.sh recommend --security 128 --performance secure
```

## 🔧 曲线别名支持

为了使用方便，bECCsh 支持多种曲线别名：

| 别名 | 对应曲线 |
|------|----------|
| p-256, prime256v1 | secp256r1 |
| p-384, prime384v1 | secp384r1 |
| p-521, prime521v1 | secp521r1 |
| btc, bitcoin, ethereum | secp256k1 |

```bash
# 这些命令效果相同
./becc_multi_curve.sh keygen -c secp256r1 -f key1.pem
./becc_multi_curve.sh keygen -c p-256 -f key2.pem
./becc_multi_curve.sh keygen -c prime256v1 -f key3.pem
```

## 📊 性能特征

### 计算效率排名 (从快到慢)

1. **secp192k1** - 最快速，适合物联网
2. **secp256k1** - 快速，加密货币标准
3. **secp224k1** - 中等速度
4. **secp256r1** - 标准速度，最广泛使用
5. **brainpoolp256r1** - 稍慢，但参数透明
6. **secp384r1** - 较慢，高安全性
7. **brainpoolp384r1** - 慢，欧洲高安全标准
8. **secp521r1** - 最慢，最高安全级别
9. **brainpoolp512r1** - 非常慢，欧洲最高标准

### 安全级别对比

| 安全级别 | 推荐曲线 | 适用场景 |
|----------|----------|----------|
| 96位 | secp192k1 | 物联网、轻量级设备 |
| 112位 | secp224k1 | 传统应用兼容 |
| 128位 | secp256r1, secp256k1, brainpoolp256r1 | 现代应用标准 |
| 192位 | secp384r1, brainpoolp384r1 | 企业级、政府应用 |
| 256位 | secp521r1, brainpoolp512r1 | 长期保密、顶级机密 |

## 🛡️ 安全建议

### 现代应用推荐
- **默认选择**: `secp256r1` (P-256)
- **加密货币**: `secp256k1` 
- **高安全性**: `secp384r1` (P-384)
- **最高安全**: `secp521r1` (P-521)

### 兼容性考虑
- **TLS 1.3**: 使用 `secp256r1` 或 `secp384r1`
- **比特币/以太坊**: 使用 `secp256k1`
- **欧洲标准**: 使用 `brainpoolp256r1`
- **物联网**: 使用 `secp192k1`

### 性能优化
- **移动设备**: 优先选择 `secp256k1` 或 `secp192k1`
- **服务器应用**: 使用 `secp256r1` 或 `secp384r1`
- **离线签名**: 可以使用 `secp521r1` 获得最高安全性

## 🔍 技术细节

### 曲线参数验证

每个曲线都包含完整的参数验证：

```bash
# 验证曲线参数
source core/crypto/curve_selector.sh
select_curve secp256r1
validate_current_curve
```

### 参数获取

```bash
# 获取曲线参数（十进制）
params=$(get_current_curve_params)

# 获取曲线参数（十六进制）
params_hex=$(get_current_curve_params_hex)

# 获取曲线详细信息
info=$(get_current_curve_info)
```

## 🧪 测试和验证

### 运行完整测试套件

```bash
# 测试所有功能
./test_multi_curve.sh

# 测试特定功能
./test_multi_curve.sh selector    # 曲线选择器
./test_multi_curve.sh params      # 参数验证
./test_multi_curve.sh recommend   # 推荐功能
./test_multi_curve.sh perf        # 性能测试
```

### 性能基准测试

```bash
# 测试所有曲线性能
./benchmark_multi_curve.sh

# 测试特定曲线
./benchmark_multi_curve.sh -c secp256k1

# 自定义迭代次数
./benchmark_multi_curve.sh -n 200

# 保存测试结果
./benchmark_multi_curve.sh -o results.txt
```

## 📚 相关文档

- [ECC算法扩展计划](ECC_ALGORITHM_EXPANSION_PLAN.md) - 详细的扩展规划
- [技术实现](TECHNICAL_IMPLEMENTATION.md) - 技术细节
- [安全检查](SECURITY_IMPROVEMENTS.md) - 安全考虑
- [性能报告](FINAL_OPENSSL_COMPARISON_REPORT.md) - 与OpenSSL对比

## 🤝 贡献指南

### 添加新曲线

1. 在 `core/curves/` 目录创建新的参数文件
2. 更新 `core/crypto/curve_selector.sh` 中的支持列表
3. 添加相应的测试用例
4. 更新文档和说明

### 测试新功能

```bash
# 运行完整测试
./test_multi_curve.sh all

# 检查兼容性
./test_multi_curve.sh compat

# 性能回归测试
./benchmark_multi_curve.sh
```

## ⚠️ 重要警告

**安全警告**: 这是一个纯Bash实现的教育和研究项目，不适合生产环境使用。在生产环境中，请使用经过充分测试的密码学库，如OpenSSL、Libsodium等。

## 🎉 总结

bECCsh 现在提供了完整的椭圆曲线密码学支持，包括：

- ✅ 9种标准椭圆曲线
- ✅ 智能曲线推荐系统
- ✅ 完整的测试套件
- ✅ 性能基准测试
- ✅ 详细的文档说明
- ✅ 友好的命令行界面

无论是学习椭圆曲线密码学，还是进行相关研究，bECCsh 都提供了丰富的功能和灵活的选择。

---

**🎯 让密码学学习变得简单有趣！**